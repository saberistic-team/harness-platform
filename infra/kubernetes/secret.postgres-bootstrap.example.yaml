apiVersion: v1
kind: Secret
metadata:
  name: harness-postgres-bootstrap
  namespace: harness
  annotations:
    harness.dev/example-only: "true"
    harness.dev/replace-before-apply: "all REPLACE_* values"
type: Opaque
stringData:
  POSTGRES_PASSWORD: REPLACE_POSTGRES_BOOTSTRAP_PASSWORD
