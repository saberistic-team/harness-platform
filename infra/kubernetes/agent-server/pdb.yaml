apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: harness-agent-server
  namespace: harness
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agent-server
      app.kubernetes.io/part-of: harness
